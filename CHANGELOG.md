# 修改日志

## 2026-07-08 多语言切换收尾与核心播放功能保护

本次更新结合本地 `agent.md` 第 9 节记录整理。`agent.md` 仍作为本地工作记录文件保留，不上传到 GitHub。

### 新增

- 新增前端多语言切换系统，支持：
  - 简体中文；
  - 繁體中文；
  - English；
  - 日本語；
  - 한국어。
- 新增语言选择保存能力，用户选择会保存在浏览器本地，下次打开继续使用。
- 登录页新增语言选择器。
- 播放器顶部新增语言选择器。
- 新增统一翻译函数和界面刷新逻辑，用于集中管理按钮、输入框、标题、提示文案等文本。

### 变更

- 将主要界面文字接入多语言，包括登录框、搜索框、个人中心、后台管理、歌单创建/选择、收藏列表、同步曲库按钮、当前播放定位按钮、播放模式提示、常见错误提示和 Toast 提示。
- 切换语言后会自动刷新当前列表，并尽量保持当前播放歌曲高亮状态。
- 修复上一轮多语言半成品误覆盖核心播放逻辑的问题，保留并确认以下功能仍存在：
  - 上次播放歌曲恢复；
  - 当前播放歌曲高亮；
  - 歌曲列表智能居中；
  - “定位当前播放”按钮；
  - 同步曲库按钮状态文案。
- 修复播放模式按钮的语言适配，覆盖列表循环、单曲循环、随机播放。
- 新增语言选择器样式，让登录页和顶部栏显示更协调。
- 更新前端资源版本号，避免浏览器继续使用旧版 `app.js` / `style.css`。
- Service Worker 缓存版本更新到 `musiccloud-v9`。

### 修复

- 修复半成品改动中可能出现的中文和图标乱码风险。
- 修复核心函数被误删导致上次播放恢复或保存可能失效的问题。
- 修复播放模式按钮切换后标题可能变空的问题。
- 修复部分简体中文词条残留英文或异常占位的问题。

### 验证

- `frontend/app.js` 语法检查通过。
- `frontend/sw.js` 语法检查通过。
- Git 空白检查通过。
- 已确认 5 种语言词条数量一致，均为 101 条。
- 已确认登录页和播放器页语言选择框存在。
- 已确认核心函数仍存在：上次播放恢复、播放状态保存、当前播放高亮、歌曲列表滚动容器识别、界面翻译刷新。

## 2026-07-08 播放恢复、歌曲定位与频谱性能优化

本次更新结合本地 `agent.md` 工作记录整理，但 `agent.md` 本身已改为本地工作记录文件，不再上传到 GitHub。

### 新增

- 新增根目录 `.gitignore`，忽略本地工作记录 `agent.md`。
- 新增 `frontend/visualizer-worker.js`，将底部节律条 / 频谱条绘制迁移到 Worker 后台线程，减轻浏览器主线程压力。
- 新增用户最后播放歌曲记录能力：
  - 数据库 `Users` 表新增 `LastSongID` 字段；
  - 后端新增读取最后播放歌曲接口；
  - 后端新增更新最后播放歌曲接口。

### 变更

- 前端播放歌曲时会保存当前歌曲，优先同步到后端，支持同一账号跨设备恢复上次播放歌曲。
- 页面启动后会尝试恢复上次播放歌曲，加载播放器并定位到歌曲所在列表，但保持暂停，不自动播放。
- 重构“当前播放歌曲定位”逻辑，自动识别真实滚动容器：桌面端优先滚动 `center-panel`，避免滚错 `center-list` 导致无法居中。
- 重构播放高亮逻辑，每次播放前清理旧的 `.playing`，再只给当前歌曲添加高亮，避免绿色播放标记停留在上一首。
- “定位当前播放”按钮改为优先寻找当前歌曲行；找不到时会回到歌曲所在文件夹，重新渲染后再定位并闪烁提示。
- 底部节律条增加 Worker 后台绘制和主线程兜底方案；不支持 Worker / OffscreenCanvas 时仍可正常显示。
- 移动端继续关闭节律条绘制，减少移动设备性能消耗。
- 更新前端资源版本号，避免浏览器继续使用旧缓存。
- Service Worker 缓存版本更新，并加入 `visualizer-worker.js` 缓存。
- 修复地址栏偏移设置控件不存在时可能引发的初始化错误。
- 本地 `agent.md` 从 Git 跟踪中移除，仅保留在本机用于调试记录。

### 验证

- `frontend/app.js` 语法检查通过。
- `frontend/sw.js` 语法检查通过。
- Git 空白检查通过。
- 已按 `agent.md` 记录进行浏览器实测：播放高亮唯一、歌曲定位按钮可用、切歌后自动居中、刷新后恢复最后播放歌曲且保持暂停。

## 2026-07-07 前端性能与视觉体验优化

本次更新根据 `agent.md` 的工作记录，提交歌词滚动、底部节律条和前端体验优化，并保留本轮调试记录。

### 新增

- 新增 `agent.md`，用通俗语言记录本轮发现的问题、修改原因、涉及文件和验证情况。

### 变更

- 优化歌词滚动性能：缓存歌词行元素，只更新上一句和当前句，减少反复扫描 DOM。
- 歌词定位改用更高效的查找方式，降低播放时的 CPU 压力。
- 移动端歌词滚动减少频繁布局读取，降低低配电脑或移动设备上的卡顿概率。
- 当前歌词高亮改用缩放效果，避免通过字号变化引发大范围页面重排。
- 优化底部节律条 / 频谱条：加入音量自适应、区间平均、高频补偿和缓动效果，减少“冲顶”现象，增强节奏层次。
- 调整相关样式，让歌词与节律条动画更平稳。

### 验证

- `frontend/app.js` 语法检查通过。
- `frontend/sw.js` 语法检查通过。
- Git 空白检查通过。

## 2026-07-07 路径稳定性修复

### 变更

- 后端启动时改为基于文件所在目录计算项目路径，避免从不同工作目录启动时找不到 `.env`、前端目录、封面目录或头像目录。
- `scanner.py` 改为从后端目录明确加载 `.env`，并使用相对后端目录的 `covers` 路径。
- 修复 `rebuild_covers.py` 文档字符串里的 Windows 路径转义警告。

## 2026-07-07 仓库清理与安全加固

本次更新主要完成项目依赖补全、Git 仓库瘦身、安全加固与仓库卫生整理。

### 新增

- 新增本文件 `CHANGELOG.md`，用于记录项目后续重要变更。

### 变更

- 补全后端运行依赖：
  - `python-dotenv`
  - `httpx`
  - `apscheduler`
- 更新 `backend/.gitignore`，将运行时生成的封面目录加入忽略：
  - `covers/`
- 重建本地 Git 历史，移除历史中误提交的大体积封面文件。
- 将仓库历史整理为干净的主线提交，降低仓库体积。

### 安全

- 检查 `admin` 管理员账号是否仍使用默认密码 `123456`。
- 已确认默认密码曾存在，并已重置为强随机密码。
- 未将新密码写入仓库文件，避免敏感信息泄露。

### 仓库维护

- `backend/covers/` 现在作为本地运行数据保留，不再进入 Git 版本控制。
- 清理后本地 `.git` 目录从约 381 MB 降至约 4 MB。
- 注意：本次 GitHub 同步需要改写远程历史。如果其他设备或协作者已有旧仓库副本，应重新克隆或重置到新的 `main` 分支。

